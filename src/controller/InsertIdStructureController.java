package controller;

/**
 * Created by tizianoditoma on 13/09/16.
 */
public class InsertIdStructureController {

    public static boolean insertIdStructureController(Integer idStructure) {
        /*
        Connection connection = DataSource.getConnection();
        try {
            PreparedStatement preparedStatement = connection.prepareStatement(Query.addIdLocation);
            preparedStatement.setInt(1,idStructure);

            preparedStatement.execute();
            preparedStatement.clearParameters();
        } catch (SQLException e){
            e.printStackTrace();;
            JOptionPane.showMessageDialog(null, "SQL Error occurs!");
            return  false;
        }
    */
        return true;
    }
}
